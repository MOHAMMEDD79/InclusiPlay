<!-- MainWindow.xaml -->
<Window x:Class="InclusiPlay.Mix"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Science Experiment Simulator" Height="450" Width="800">
    <Grid>

        <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
            <Button x:Name="pre"                          
                            BorderThickness="0"
                            Content="Pre"
                            Foreground="White"
                            FontSize="12"
                            FontFamily="Montserrat"
                            Cursor="Hand" Width="350" Click="pre_Click2" RenderTransformOrigin="0.5,0.5" Margin="381,-13,10,-11"                        
                          
                           >
                <Button.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform X="-500" Y="40"/>
                    </TransformGroup>
                </Button.RenderTransform>
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Background" Value="#462AD8"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#28AEED"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Button.Template>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Width="100" Height="40"
                                        CornerRadius="20"
                                        Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <ComboBox x:Name="ChemicalComboBox" 
                  ItemsSource="{Binding AvailableChemicals}" 
                  DisplayMemberPath="Name" 
                  SelectedValuePath="Name" 
                  Grid.Row="0" Width="200" Margin="5"/>
        <Button Content="Add Chemical" 
                Command="{Binding AddChemicalCommand}" 
                CommandParameter="{Binding ElementName=ChemicalComboBox, Path=SelectedValue}" 
                Grid.Row="1" Width="150" Height="50" Margin="10"/>

        <ListBox ItemsSource="{Binding Chemicals}" 
                 Grid.Row="2" 
                 HorizontalContentAlignment="Stretch" 
                 VerticalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Width="100" Height="50" Margin="5"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>

        <Button Content="Mix Chemicals" 
                Command="{Binding MixChemicalsCommand}" 
                Grid.Row="3" Width="150" Height="50" Margin="10"/>

        <!-- Red-colored box for displaying experiment results -->
        <Border BorderBrush="Red" 
                BorderThickness="2" 
                Grid.Row="4" Margin="10">
            <TextBlock Text="{Binding ExperimentResult}" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       FontSize="16" 
                       Foreground="Black"/>
        </Border>

        <!-- Main content -->
        <TextBlock Text="Welcome to the Science Experiment Simulator!" 
                   Grid.Row="5" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   FontSize="20"/>
                
    </Grid>
    </Grid>
</Window>
